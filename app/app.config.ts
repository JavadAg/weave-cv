export default defineAppConfig({
  ui: {
    colors: {
      neutral: "zinc"
    },
    switch: {
      slots: {
        root: "flex-row-reverse justify-between",
        wrapper: "ms-0 me-2"
      }
    }
  }
})
